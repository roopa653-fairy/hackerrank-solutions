/*
Problem: Breaking the Records
Platform: HackerRank
Difficulty: Easy

Logic:
Initialize the highest and lowest scores with the first game score.
Traverse the score list starting from the second game.
Each time a score exceeds the current highest score, increment the highest record count.
Each time a score is lower than the current lowest score, increment the lowest record count.
Return the counts of highest and lowest records broken.

Time Complexity: O(n)
Space Complexity: O(1)
*/
public static List<Integer> breakingRecords(List<Integer> scores) {
    // Write your code here
        int countHighest=0;
        int countLowest=0;
        int Highscore=scores.get(0);
        int Lowscore=scores.get(0);
        for(int i=1;i<scores.size();i++){
            if(scores.get(i)>Highscore){
                Highscore=scores.get(i);
                countHighest++;
            }
            else if(scores.get(i)<Lowscore){
                Lowscore=scores.get(i);
                countLowest++;
            }
        }
        return Arrays.asList(countHighest,countLowest);

    }
